<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:MauiApp1"
             x:Class="MauiApp1.DoporucenaCetba"
             >

    <ContentPage.Content>
        <CollectionView ItemsSource="{Binding BookCategories}" SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Padding="10" Spacing="10">
                        <Label Text="{Binding Name}" FontSize="20" FontAttributes="Bold" />

                        <CollectionView ItemsSource="{Binding Books}" SelectionMode="None">
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <HorizontalStackLayout Spacing="10">
                                        <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                  CheckedChanged="OnBookCheckedChanged" />

                                        <Label Text="{Binding Title}" FontSize="16" VerticalOptions="Center" />
                                    </HorizontalStackLayout>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </ContentPage.Content>
</ContentPage>
